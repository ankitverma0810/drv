import React from "react";

import { Standard } from "../Textfield/Textfield.stories";

export default {
    title: "Form/Subscription"
};

export const StandardSubscription = () => {
    return (
        <>
            <Standard />
        </>
    );
};
